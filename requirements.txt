flask>=3.0.0
flask-socketio>=5.3.6
anthropic>=0.25.0
requests>=2.31.0
python-dotenv>=1.0.0
eventlet>=0.35.0
# Optional: pip install google-generativeai (for Gemini)
# Optional: ollama runs as local server, no pip install needed
